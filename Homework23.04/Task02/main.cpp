/*2.	Користувач вводить текст. Виведіть цей текст на екран.
Складність задачі в тому, що текст має вводиться лише через scanf_s.
Проблема цієї функції, що вона може зчитувати лише одне слово.
Ваша ціль викликати її декілька разів. Скільки саме, має розібратись.*/

#include <stdio.h>
#include <string.h>
int main()
{
    char text[100] = "";
    char word[30];
    while (scanf("%s", word) == 1)
    {
        strcat(text, word);
    }

    printf("%s", text);

    return 0;
}